// ignore_for_file: file_names

void main() {
  int nilai = 75; // Nilai murid
//sama dengan if statement

  // Menggunakan ternary operator untuk menentukan kelulusan
  String hasil = (nilai >= 60) ? 'Anda lulus' : 'Anda tidak lulus';

  print(hasil);
}
